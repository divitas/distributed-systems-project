chunk_size: 256

local_cpu: true
max_local_cpu_size: 5.0

local_disk: null
max_local_disk_size: 0.0

save_decode_cache: false

# If LMCACHE_CONFIG_FILE is set, LMCache uses this file instead of
# LMCACHE_* environment-variable config values.
